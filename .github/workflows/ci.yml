name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: pyramide
          POSTGRES_PASSWORD: pyramide
          POSTGRES_DB: pyramide
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U pyramide"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10

    env:
      DATABASE_URL: postgresql://pyramide:pyramide@localhost:5432/pyramide
      AUTH_SECRET: ci-only-secret
      NEXTAUTH_URL: http://localhost:3000

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - run: npm ci

      - name: Lint
        run: npm run lint

      - name: Typecheck
        run: npx tsc --noEmit

      - name: Unit tests
        run: npm run test

      - name: Migration dry-run against a fresh database
        run: npm run db:migrate

      - name: Next.js production build
        run: npm run build

      - name: Server bundle build (migrate/worker)
        run: npm run build:server
